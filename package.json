{
  "name": "toast-app-backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply toast-app-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply toast-app-db",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "drizzle-kit": "^0.31.5",
    "drizzle-orm": "^0.44.6",
    "hono": "^4.10.0"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "wrangler": "^4.4.0"
  }
}
